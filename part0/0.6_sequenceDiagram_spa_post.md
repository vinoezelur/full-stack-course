sequenceDiagram
    participant browser
    participant server
    

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa with the new note as JSON data
    activate server
    server-->>browser: Status code 201 created
    deactivate server

    Note right of browser: The browser adds the note to the list, rerenders the note list on the page and sends the new note to the server.